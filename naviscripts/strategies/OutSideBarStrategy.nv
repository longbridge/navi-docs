strategy("OutSide Bar Strategy", overlay: true);
if (high > high[1] and low < low[1]) {
    if (close > open) {
        strategy.entry("OutBarLE", Direction.Long, comment: "OutBarLE");
    }
    if (close < open) {
        strategy.entry("OutBarSE", Direction.Short, comment: "OutBarSE");
    }
}
