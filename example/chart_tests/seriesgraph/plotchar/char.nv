// 正确结果：每 10 根柱子出现一次自定义字符"▲"，显示在柱子上方
indicator("Chart Test: plotchar/char", overlay: true);

let trigger = bar_index % 10 == 0;
plot_char(trigger, "Arrow", char: "▲", location: Location.AboveBar, color: Color.BLUE, size: Size.Normal);
