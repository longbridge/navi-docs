// 正确结果：每 15 根柱出现字符"▲"，文字标注"Buy"显示在字符正下方（BelowBar 时文字在下）
indicator("Chart Test: plotchar/text", overlay: true);

let trigger = bar_index % 15 == 0;
plot_char(trigger, "Signal", char: "▲", location: Location.BelowBar,
    color: Color.GREEN, text: "Buy", text_color: Color.GREEN, size: Size.Small);
